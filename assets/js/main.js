/*
// Format JSON :
let monObjetJSON = '{"prop1" : "valeur1" , "prop2" : "valeur2"}';

// Format Objet
let monObjet {
    prop1: 'valeur1',
    prop2: 'valeur2',
}

// Transformer un objet en chaine de caracteres
let objectString = JSON.stringify(monObjet);

// Transformer une chaine JSON en objet
let object = JSON.parse(monObjetJSON);
*/

/* Notes :
    - localStorage = https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage
*/